framework:
    cache:
        # Use Redis for application cache
        app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_URL)%'

        # Configure specific pool for rate limiter
        pools:
            cache.rate_limiter:
                adapter: cache.adapter.redis
                default_lifetime: 900  # 15 minutes (matches rate limiter interval)
