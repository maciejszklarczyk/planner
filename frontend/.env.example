# Project name (for container naming)
PROJECT_NAME=plan

# Next.js API URL (for local development only)
# In production, this is set at Docker build time via GitLab CI
# See .gitlab-ci.yml: NEXT_PUBLIC_API_URL variable
NEXT_PUBLIC_API_URL=http://localhost:8000

# Node environment
NODE_ENV=production
PORT=3000
