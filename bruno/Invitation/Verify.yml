info:
  name: Verify
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/invitation/verify"
  params:
    - name: token
      value: "your-raw-token-here"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
