info:
  name: Edit User
  type: http
  seq: 2

http:
  method: PUT
  url: "{{url}}/user"
  body:
    type: json
    data: |-
      {
        "name": "New Name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
