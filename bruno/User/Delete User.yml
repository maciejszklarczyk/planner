info:
  name: Delete User
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/user/:userId"
  params:
    - name: userId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
