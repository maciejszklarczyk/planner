info:
  name: Konkretna grupa
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/groups/:groupId"
  params:
    - name: groupId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
