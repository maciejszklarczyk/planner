info:
  name: Usunięcie grupy
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/groups/:groupId"
  params:
    - name: groupId
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
