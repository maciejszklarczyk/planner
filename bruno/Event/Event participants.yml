info:
  name: Event participants
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/events/:event/participants"
  params:
    - name: event
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
