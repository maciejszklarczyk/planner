info:
  name: Event
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/events/:event"
  params:
    - name: event
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
