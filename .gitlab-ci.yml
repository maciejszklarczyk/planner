stages:
  - trigger

backend:
  stage: trigger
  trigger:
    include:
      - local: 'backend/.gitlab-ci.yml'
    strategy: depend

frontend:
  stage: trigger
  trigger:
    include:
      - local: 'frontend/.gitlab-ci.yml'
    strategy: depend
